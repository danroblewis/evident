; prev_increment — golden next-state witness
; Captured from: evident test runtime-contract/fixtures/prev_increment/source.ev
; Claim: sat_subsequent_tick_increments
; Golden: count = 8, state_next = Done

(assert (= count 8))
(assert (= state_next Done))
