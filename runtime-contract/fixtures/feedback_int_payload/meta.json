{
  "format_version": 1,
  "name": "feedback_int_payload",
  "fsm_claim": "accumulator",
  "source_ev": "runtime-contract/fixtures/feedback_int_payload/source.ev",
  "from_example": null,
  "from_claim": "sat_ready_increments",
  "how_built": "handwritten",
  "notes": "last_results threading at the Int-PAYLOAD level (not just variant): n = IntResult(41) payload → 41, used numerically in IntToStr(n + 1) → IntToStr(42). Pins state = AReady, last_results = [IntResult(41)]; the ParseInt on the prior tick is what produced that IntResult in a live run, captured here as the pinned input frame. effects_in_smt:true so both SMT engines decode the resulting effect from the model.",

  "state_var":        "state",
  "state_next_var":   "state_next",
  "effects_var":      "effects",
  "last_results_var": "last_results",
  "world_fields": [],

  "given": {
    "state":        {"enum": "AState", "variant": "AReady", "fields": []},
    "last_results": {"seq_enum": "Result", "elems": [
      {"enum": "Result", "variant": "IntResult", "fields": [{"int": 41}]}
    ]}
  },

  "expect": {
    "unsat": false,
    "model": {
      "state_next": {"enum": "AState", "variant": "ADone", "fields": []}
    },
    "effects": [
      {"enum": "Effect", "variant": "IntToStr", "fields": [{"int": 42}]}
    ],
    "halt": false,
    "exit_code": null
  },

  "effects_in_smt": true
}
