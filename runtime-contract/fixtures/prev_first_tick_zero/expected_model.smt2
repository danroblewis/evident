; prev_first_tick_zero — golden next-state witness
; Captured from: evident test runtime-contract/fixtures/prev_first_tick_zero/source.ev
; Claim: sat_first_tick_count_is_zero
; Golden: count = 0, state_next = Counting

(assert (= count 0))
(assert (= state_next Counting))
