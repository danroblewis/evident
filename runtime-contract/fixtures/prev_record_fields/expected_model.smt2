; prev_record_fields — golden next-state witness
; Captured from: evident test runtime-contract/fixtures/prev_record_fields/source.ev
; Claim: sat_independent_field_pins
; Golden: pos.x = 8, pos.y = 13

(assert (= |pos.x| 8))
(assert (= |pos.y| 13))
