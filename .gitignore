.pytest_cache/
parser/tests/__pycache__/
parser/src/__pycache__/
__pycache__/
plotly.min.js
d3.min.js
.claude/worktrees/
a.smt

# local dev-container/bot config
.claudebot
*.profraw

# stray images dropped at the repo ROOT — sample portraits, or a browser screenshot
# whose filename forgot the recordings path. Never committed; screenshots belong in
# ide/critics/recordings/. (Root-only: /*.png does not touch viz/ or subdir images.)
/portrait_*.svg
/*.png
/*.jpeg

# generated visualization artifacts (regenerated by viz/contact_sheet.py)
viz/gallery/
viz/ir/
viz/CONTACT_SHEET.md
viz/combo/

# playwright MCP browser output (the ide-critic's session artifacts)
.playwright-mcp/
# critic session recordings (screenshots + session logs — watch later, never committed)
ide/critics/recordings/
/ide/tasks.json.lock
