import "stdlib/kernel.ev"
claim Outer
    shared_const ∈ Int = 42
type Holder(local ∈ Int)
    sum ∈ Int = local + shared_const
claim main
    ..Outer
    h ∈ Holder (local ↦ 8)
    h.sum = 50
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
