import "stdlib/kernel.ev"
claim wants_big
    secret ∈ Int
    secret > 10
claim wants_small
    secret ∈ Int
    secret < 5
claim main
    n ∈ Int = 1
    wants_big
    wants_small
    effects ∈ Seq(Effect) = ⟨Exit(n - 1)⟩
