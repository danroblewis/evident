import "stdlib/kernel.ev"
claim pick
    picked ∈ Int
    out ∈ Int
    out = picked
    picked > 5
claim main
    a ∈ Int
    b ∈ Int
    pick(out ↦ a)
    pick(out ↦ b)
    a = 6
    b = 9
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
