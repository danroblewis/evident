import "stdlib/kernel.ev"
enum Tok2 =
    TIdent(String)
    TInt(Int)
    TComma
enum Wrap2 = WOne(Tok2) | WNone
claim main
    w ∈ Wrap2 = WOne(TComma)
    ok ∈ Bool = (w matches WOne(_))
    effects ∈ Seq(Effect) = ⟨Exit(ok ? 0 : 1)⟩
