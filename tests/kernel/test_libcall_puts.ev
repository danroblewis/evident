-- LibCall via real libffi: libc's puts.
-- expect: stdout = "hello via libffi!"
-- expect: exit = 0
import "stdlib/kernel.ev"

claim main
    mode ∈ String = "Done"
    effects ∈ Seq(Effect) = ⟨
        LibCall("libc", "puts", ⟨ArgStr("hello via libffi!")⟩),
        Exit(0)
    ⟩
