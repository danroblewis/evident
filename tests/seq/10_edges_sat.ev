-- C10 consecutive pairs ∀ (a,b) ∈ edges(xs) : P — adjacent elements of a bounded seq.
-- entry: main
-- expect: exit = 0
import "stdlib/kernel.ev"
claim main
    xs ∈ Seq(Int) = ⟨1, 2, 3⟩
    ∀ (a, b) ∈ edges(xs) : a < b
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
