-- DriverClaimIdx isolation — claim-index append counter.
-- Skip pass held with a claim head (head_is_claim, tok1 an Ident) so
-- claimindex_add fires each tick; exit when the count reaches 3. The
-- BuildMemWriteLong index-write effect is not scheduled (no arena).
-- entry: main
-- expect: stdout = "3"
-- expect: exit = 0
import "stdlib/kernel.ev"
import "stdlib/z3_ast.ev"
import "stdlib/z3_datatypes.ev"
import "compiler/lexer.ev"
import "compiler/parser.ev"
import "compiler2/driver_ir.ev"
import "compiler2/driver_expr.ev"
import "compiler2/driver_claimidx.ev"

fsm main
    enter_skip ∈ Bool
    enter_skip = (is_first_tick ? true : _enter_skip)
    head_is_claim ∈ Bool
    head_is_claim = (is_first_tick ? true : _head_is_claim)
    tok1 ∈ Token
    tok1 = (is_first_tick ? Ident("x") : _tok1)
    decl_name ∈ String
    decl_name = (is_first_tick ? "x" : _decl_name)
    effects_cond ∈ Bool
    effects_cond = (is_first_tick ? false : _effects_cond)
    claimindex_buf ∈ FtiBuffer
    claimindex_buf.base = (is_first_tick ? 0 : _claimindex_buf.base)
    enter_enum_decl ∈ Bool
    enter_enum_decl = (is_first_tick ? false : _enter_enum_decl)
    enter_efflit ∈ Bool
    enter_efflit = (is_first_tick ? false : _enter_efflit)
    work_nil ∈ Bool
    work_nil = (is_first_tick ? false : _work_nil)
    decl_action ∈ Int
    decl_action = (is_first_tick ? 0 : _decl_action)
    user_decl_done ∈ Bool
    user_decl_done = (is_first_tick ? false : _user_decl_done)
    in_parse ∈ Bool
    in_parse = (is_first_tick ? false : _in_parse)
    parse_ready ∈ Bool
    parse_ready = (is_first_tick ? false : _parse_ready)
    parse_kind ∈ Int
    parse_kind = (is_first_tick ? 0 : _parse_kind)
    parse_mode ∈ Int
    parse_mode = (is_first_tick ? 0 : _parse_mode)
    parse_bad ∈ Bool
    parse_bad = (is_first_tick ? false : _parse_bad)
    tok_cur ∈ Int
    tok_cur = (is_first_tick ? 0 : _tok_cur)
    tok_ready ∈ Bool
    tok_ready = (is_first_tick ? false : _tok_ready)
    user_enum_name ∈ String
    user_enum_name = (is_first_tick ? "" : _user_enum_name)
    tok0 ∈ Token
    tok0 = (is_first_tick ? EofTok : _tok0)
    tok2 ∈ Token
    tok2 = (is_first_tick ? EofTok : _tok2)
    tok3 ∈ Token
    tok3 = (is_first_tick ? EofTok : _tok3)
    tok4 ∈ Token
    tok4 = (is_first_tick ? EofTok : _tok4)
    tok5 ∈ Token
    tok5 = (is_first_tick ? EofTok : _tok5)
    tok6 ∈ Token
    tok6 = (is_first_tick ? EofTok : _tok6)
    z3_argint_decl ∈ Int
    z3_argint_decl = (is_first_tick ? 0 : _z3_argint_decl)
    z3_argstr_decl ∈ Int
    z3_argstr_decl = (is_first_tick ? 0 : _z3_argstr_decl)
    z3_cell_decl ∈ Int
    z3_cell_decl = (is_first_tick ? 0 : _z3_cell_decl)
    z3_empty_val ∈ Int
    z3_empty_val = (is_first_tick ? 0 : _z3_empty_val)
    z3_libcall_decl ∈ Int
    z3_libcall_decl = (is_first_tick ? 0 : _z3_libcall_decl)
    ..DriverClaimIdx
    c_msg ∈ String = str_from_int(claimindex_buf.count)
    effects ∈ Seq(Effect) = ((claimindex_buf.count = 3)
        ? ⟨LibCall("libc", "puts", ⟨ArgStr(c_msg)⟩), Exit(0)⟩
        : ⟨LibCall("libc", "getpid", ⟨⟩)⟩)
