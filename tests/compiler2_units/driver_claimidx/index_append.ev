-- DriverClaimIdx isolation — claim-index append counter.
--
-- Drives the skip pass (d_enter_skip held, a claim head: d_head_is_claim
-- with ww_t1 an Ident) so ci_add fires every tick and the claim index
-- counter ci_cnt climbs. Exit when ci_cnt reaches 3, reporting it. This
-- exercises the ci_add recognizer + the bounded ci_cnt append; if the
-- recognizer or the counter regresses the count never reaches 3. The
-- BuildMemWriteLong index-write effect is not scheduled (no arena).
--
-- entry: main
-- expect: stdout = "3"
-- expect: exit = 0
import "stdlib/kernel.ev"
import "stdlib/z3_ast.ev"
import "stdlib/z3_datatypes.ev"
import "compiler/lexer.ev"
import "compiler/parser.ev"
import "compiler2/driver_ir.ev"
import "compiler2/driver_expr.ev"
import "compiler2/driver_claimidx.ev"

fsm main
    d_enter_skip ∈ Bool
    d_enter_skip = (is_first_tick ? true : _d_enter_skip)
    d_head_is_claim ∈ Bool
    d_head_is_claim = (is_first_tick ? true : _d_head_is_claim)
    ww_t1 ∈ Token
    ww_t1 = (is_first_tick ? Ident("x") : _ww_t1)
    d_cl_name ∈ String
    d_cl_name = (is_first_tick ? "x" : _d_cl_name)
    c_eff_cond ∈ Bool
    c_eff_cond = (is_first_tick ? false : _c_eff_cond)
    cibuf ∈ FtiBuffer
    cibuf.base = (is_first_tick ? 0 : _cibuf.base)
    d_enter_edecl ∈ Bool
    d_enter_edecl = (is_first_tick ? false : _d_enter_edecl)
    d_enter_el ∈ Bool
    d_enter_el = (is_first_tick ? false : _d_enter_el)
    d_witems_nil ∈ Bool
    d_witems_nil = (is_first_tick ? false : _d_witems_nil)
    ed_act ∈ Int
    ed_act = (is_first_tick ? 0 : _ed_act)
    ed_done_user ∈ Bool
    ed_done_user = (is_first_tick ? false : _ed_done_user)
    in_parse ∈ Bool
    in_parse = (is_first_tick ? false : _in_parse)
    p_done ∈ Bool
    p_done = (is_first_tick ? false : _p_done)
    pk_kind ∈ Int
    pk_kind = (is_first_tick ? 0 : _pk_kind)
    pmode ∈ Int
    pmode = (is_first_tick ? 0 : _pmode)
    pr_bad ∈ Bool
    pr_bad = (is_first_tick ? false : _pr_bad)
    tcur ∈ Int
    tcur = (is_first_tick ? 0 : _tcur)
    tok_ready ∈ Bool
    tok_ready = (is_first_tick ? false : _tok_ready)
    ue_name ∈ String
    ue_name = (is_first_tick ? "" : _ue_name)
    ww_t0 ∈ Token
    ww_t0 = (is_first_tick ? EofTok : _ww_t0)
    ww_t2 ∈ Token
    ww_t2 = (is_first_tick ? EofTok : _ww_t2)
    ww_t3 ∈ Token
    ww_t3 = (is_first_tick ? EofTok : _ww_t3)
    ww_t4 ∈ Token
    ww_t4 = (is_first_tick ? EofTok : _ww_t4)
    ww_t5 ∈ Token
    ww_t5 = (is_first_tick ? EofTok : _ww_t5)
    ww_t6 ∈ Token
    ww_t6 = (is_first_tick ? EofTok : _ww_t6)
    z_argint_decl ∈ Int
    z_argint_decl = (is_first_tick ? 0 : _z_argint_decl)
    z_argstr_decl ∈ Int
    z_argstr_decl = (is_first_tick ? 0 : _z_argstr_decl)
    z_cell_decl ∈ Int
    z_cell_decl = (is_first_tick ? 0 : _z_cell_decl)
    z_empty_val ∈ Int
    z_empty_val = (is_first_tick ? 0 : _z_empty_val)
    z_lc_decl ∈ Int
    z_lc_decl = (is_first_tick ? 0 : _z_lc_decl)
    ..DriverClaimIdx
    c_msg ∈ String = str_from_int(cibuf.count)
    effects ∈ Seq(Effect) = ((cibuf.count = 3)
        ? ⟨LibCall("libc", "puts", ⟨ArgStr(c_msg)⟩), Exit(0)⟩
        : ⟨LibCall("libc", "getpid", ⟨⟩)⟩)
