-- stage0 spike probe: does a DIGIT inside an identifier survive the
-- fossil? parsedispatch.ev round 1 drained exactly at `pd_k1 ∈ Int`
-- (a plain declare, proven shape) — digit-bearing names are the only
-- delta from every passing fossil-subset probe (p00–p70 use letter-only
-- locals). Expected if digits break: emit stops after pa declare.
import "stdlib/kernel.ev"

claim main
    pa ∈ Int = 7
    pb1 ∈ Int = 9
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
