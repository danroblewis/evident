-- Probe: String ops — `++` concat and `#s` length.
-- expect-smt2: (= t (str.++ s "c"))-style, (= n (str.len t))-style.

import "stdlib/kernel.ev"

claim main
    s ∈ String = "ab"
    t ∈ String
    t = s ++ "c"
    n ∈ Int
    n = #t
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
