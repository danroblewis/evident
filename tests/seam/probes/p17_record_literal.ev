-- Probe: record literal in an expression (`p = IVec2(1, 2)`).
-- expect-smt2: p's fields pinned to 1 and 2.

import "stdlib/kernel.ev"

type IVec2(x, y ∈ Int)

claim main
    p ∈ IVec2
    p = IVec2(1, 2)
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
