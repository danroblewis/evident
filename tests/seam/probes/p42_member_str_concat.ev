-- Probe: String `++` in a membership pin (`t ∈ String = s ++ "c"`)
-- vs round-1 p13's broken bare-assert form.

import "stdlib/kernel.ev"

claim main
    s ∈ String = "ab"
    t ∈ String = s ++ "c"
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
