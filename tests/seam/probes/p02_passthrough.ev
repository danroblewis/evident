-- Probe: `..ClaimName` passthrough inline.
-- expect-smt2: (= n 11) inlined from IsEleven; sentinel survives.

import "stdlib/kernel.ev"

claim IsEleven
    n ∈ Int
    n = 11

claim main
    n ∈ Int
    ..IsEleven
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
