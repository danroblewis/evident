-- Probe: user-defined enum declaration + ctor pin.
-- expect-smt2: Pick datatype declared, (= e (AA 7))-style pin.

import "stdlib/kernel.ev"

enum Pick = AA(Int) | BB

claim main
    e ∈ Pick = AA(7)
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
