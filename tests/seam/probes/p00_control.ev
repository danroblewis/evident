-- Probe: control — flat claim, no composition. Must compile.
-- expect-smt2: (= x 7), (= y (+ x 1)), (= sentinel 42), Exit 0

import "stdlib/kernel.ev"

claim main
    x ∈ Int = 7
    y ∈ Int
    y = x + 1
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
