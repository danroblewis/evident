-- Probe: negative literal via bare assert (`x = -7`)
-- vs round-2 p26's broken membership pin `x ∈ Int = -7`.

import "stdlib/kernel.ev"

claim main
    x ∈ Int
    x = -7
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
