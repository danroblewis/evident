-- Probe: implication with parenthesized equality consequent
-- (`flag ⇒ (n = 11)`) — the non-claim conditional constraint shape.

import "stdlib/kernel.ev"

claim main
    n ∈ Int
    flag ∈ Bool
    flag ⇒ (n = 11)
    sentinel ∈ Int = 42
    effects ∈ Seq(Effect) = ⟨Exit(0)⟩
