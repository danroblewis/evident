; Generated by Evident — claim: ValidSchedule
(declare-const task.id Int)
(assert (>= task.id 0))
(declare-const task.duration Int)
(assert (>= task.duration 0))
(declare-const task.deadline Int)
(assert (>= task.deadline 0))
(assert (< task.duration task.deadline))
(declare-const slot Int)
(assert (>= slot 0))
(declare-const budget Int)
(assert (>= budget 0))
(assert (> slot 0))
(assert (<= (+ slot task.duration) budget))
(check-sat)
(get-model)
